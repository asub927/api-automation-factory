// GENERATED by api-automation-factory — DO NOT EDIT
// Owned path: regenerated on propose. Hand edits belong in support/fixtures.
import { test, expect } from "@playwright/test";
import { listPostsResponseSchema } from "../schemas/listPosts.schema.js";
import { loadAuthHeaders } from "../../../support/fixtures/auth.js";
import { expectZod } from "../../../support/fixtures/expectZod.js";

test.describe("jsonplaceholder http listPosts", () => {
  test("GET /posts", async ({ request }) => {
    const headers = loadAuthHeaders("jsonplaceholder");
    const path = "/posts";
    const response = await request.fetch(path, {
      method: "GET",
      headers,
      data: undefined,
    });
    expect(response.ok(), `status ${response.status()}`).toBeTruthy();
    const body: unknown = await response.json();
    expectZod(listPostsResponseSchema, body);
  });
});
